<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <!-- 全局参数设置 -->
    <settings>
        <!-- 开启驼峰命名自动转换 -->
        <setting name="mapUnderscoreToCamelCase" value="true"/>
        <!-- 打印查询语句 -->
        <setting name="logImpl" value="STDOUT_LOGGING"/>
    </settings>

    <!-- 类型别名，简化映射文件中的长类名 -->
    <typeAliases>
        <package name="com.example"/>
    </typeAliases>

    <!-- 环境配置 -->
    <environments default="development">
        <environment id="development">
            <!-- 使用JDBC事务管理 -->
            <transactionManager type="JDBC"/>
            <!-- 数据库连接池配置 -->
            <dataSource type="POOLED">
                <property name="driver" value="com.mysql.cj.jdbc.Driver"/>
                <property name="url" value="jdbc:mysql://localhost:3306/studb"/>
                <property name="username" value="root"/>
                <property name="password" value="123456"/>
            </dataSource>
        </environment>
    </environments>

    <!-- 映射文件配置 -->
    <mappers>
        <!-- 方式1：使用resource指定单个映射文件 -->
        <mapper resource="mapper/EmployeeMapper.xml"/>

<!--        &lt;!&ndash; 方式2：使用class指定Mapper接口（需接口与XML同名同路径） &ndash;&gt;-->
<!--        <mapper class="com.example.mapper.ProductMapper"/>-->

<!--        &lt;!&ndash; 方式3：自动扫描包下所有Mapper接口 &ndash;&gt;-->
<!--        <package name="com.example.mapper"/>-->
    </mappers>
</configuration>